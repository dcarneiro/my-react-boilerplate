import styled from 'styled-components';

import NormalImg from '../Img';

const Img = styled(NormalImg)`
  width: 100%;
  margin: 0 auto;
  display: block;
  overflow: hidden;
`;

export default Img;
